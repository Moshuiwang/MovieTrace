# P1-G 完成报告：CLI 命令 4 条

**日期：** 2026-05-11
**执行 Agent：** Claude Code (deepseek-v4-pro)

---

## 修改清单

| 文件 | 说明 |
|------|------|
| `src/movietrace/cli.py` | 4 条 CLI 子命令（~250 行，argparse） |

## CLI 命令

1. `daily-discover [--date] [--dry-run]` — 完整日发现流水线
2. `validate-feishu` — 飞书 API 连通性验证
3. `inspect-baseline [--query] [--format]` — 本地基线查询
4. `check-feishu-schema` — 飞书表 schema 验证

## 验证

- 全量回归：✅ 276/276 通过
- inspect-baseline 输出正确（855 baseline + 89 candidates）
- daily-discover --help 正常

## 使用方式

```bash
PYTHONPATH=src python -m movietrace.cli daily-discover --dry-run
PYTHONPATH=src python -m movietrace.cli inspect-baseline
```
