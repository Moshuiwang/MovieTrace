# Agent 身份卡

- **工具：** Claude Code
- **模型：** Opus 4.7
- **会话时间：** 2026-05-12 00:35 +08 ~ 12:06 +08
- **起止 commit：** `5237b8b` → （待本次 commit）
- **运行环境：** Python 3.12 + `.venv/` + Linux

---

# 今日工作主线

## 1. 流程文档时间精度规范落地（commit `5237b8b`）

**触发：** 之前会话约定的时间精度规范未提交
**完成：** 4 个文档（TEMPLATE / journal-spec / report-format / session-checklist）统一加 HHMM 时分精度，2026-05-12 起执行
**关键发现：** 无

## 2. P1.5-B/C/D 任务包设计决策逐一澄清（Q1-Q4 已定，Q5-Q8 暂停）

**触发：** STATE.md 列出的 8 个待决策问题需要用户拍板
**完成：**
- Q1（飞书历史数据策略）→ (c) 直接删除/清空重建 ✅
- Q2（B 库旧字段处理）→ (b) drop 或重建表 ✅
- Q3（TMDb tv_id 获取来源）→ (a) 直接用 existing external_ids ✅
  - 结论改变的关键数据：实测 288/289 TV 记录有 TMDb ID（99.7%），仅 1 条日剧罗马音标题匹配失败
- Q4（低置信度处理）→ (c) 跳过聚合，记入 baseline_quality_issues ✅
- Q5（多季合并 + season_number）→ ⏳ 暂停，等待新输入源
- Q6/Q7/Q8（P1.5-D 写入粒度/轮询/集成）→ ⏳ 暂停，等待新输入源

**关键发现：**
- `canonical_items.season_number` 字段存在但 V1 未填充（289 条全 NULL）
- 675 条 TV baseline_items 中 672 条标题含 `S\d\d` 季号模式，3 条不含
- 标题解析季号方案不可靠（用户确认），输入源将从 Airtable 切换到生产环境 DB

## 3. 时间戳规范补充：全局添加时区 `+08`

**触发：** 用户提出时区需求
**完成：** journal-spec / report-format / TEMPLATE / STATE.md 全部时间字段追加 `+08` 时区偏移
**决策：** 2026-05-12 起所有文档内时间戳必须带 `+08`；文件名不加（仅排序用）

---

# 关键决策记录

1. **Q3 决策反转** — 原任务包估计 external_ids 覆盖可能不完整，实测 99.7% 覆盖率，从"回退 API"改为"直接用现有数据"
2. **输入源架构变更** — 用户决定弃用 Airtable 标题解析季号方案，切换为生产环境 DB 输入源（具体 schema 待提供）；此决策导致 Q5-Q8 暂停

---

# 当前项目状态快照

- **Phase 1.5 进度：** A ✅ / B-D 任务包已起草，Q1-Q4 已决策并更新，Q5-Q8 暂停
- **阻塞项：** 生产环境 DB schema 未确认
- **无活跃编码任务**

---

# 给下一个 AI Agent 的交接

- **可接任务：** P1.5-B 可以先做（schema migration 不受输入源变更影响，仅 Q1/Q2 已决策部分）
- **不可接任务：** P1.5-C/D —— 必须等用户提供生产 DB schema 后才能修改任务包
- **不要重做的事：** Q3 不需要回退 API（288/289 已有 TMDb ID，只有 1 条缺）；不要在 P1.5-C 里做标题解析季号
- **容易被忽略的知识：** `canonical_items.season_number` 字段已存在但 V1 从未填充；时间戳规范已全域加 `+08` 时区

---

# 数字总结

- **commit 数：** 1（`5237b8b`）+ 本次待提交
- **修改文件数：** 7（STATE.md + 3 任务包 + 3 流程文档）
- **测试用例数变化：** 不变（284，无代码改动）
