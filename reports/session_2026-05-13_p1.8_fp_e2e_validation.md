# P1.8 FlixPatrol 修复后端到端验证报告

- [ ] 报告已审阅，可归档

**验证时间：** 2026-05-13 22:19 +08  
**验证对象：** `daily-discover --date 2026-05-13 --dry-run`  
**执行工具 / 模型：** Codex（GPT-5）  
**运行环境：** Linux 6.8.0-101-generic · Python 3.12.3 · `.venv/`  
**关联修复报告：** [`reports/p1.8_a_flixpatrol_zero_data_bugfix_report.md`](p1.8_a_flixpatrol_zero_data_bugfix_report.md)

---

## 验证目标

确认 FlixPatrol 当日补数 bug 修复后，Phase 1.7 多源 discovery 端到端链路可以重新读到 FP 信号，并产出合理的 P2+ 候选分布。

本次验证重点是 FP 修复效果，不评价最终权重是否需要 Phase 1.8 调优。

---

## 执行命令

```bash
PYTHONPATH=src .venv/bin/python -m movietrace.cli daily-discover --date 2026-05-13 --dry-run
```

---

## 端到端输出

关键输出如下：

```text
MovieTrace daily-discover for 2026-05-13
Dry-run: True

[1/6] Fetching FlixPatrol... OK (120 items)
[2/6] Fetching TMDb trending... OK (13 items)
[3/6] Fetching Trakt trending... OK (25 items)
[4/6] Merging + enriching... OK (merged=724 omdb_hit=588)
[5/6] Scoring + filtering... OK (passed P2+ = 62 of 724)
[6/6] Writing content_updates + baseline tracking... OK (written=0 + 0 new_seasons)

✓ Daily discovery complete (P0=1 P1=6 P2=55)
```

---

## 与 Phase 1.7 验收对比

| 指标 | Phase 1.7 验收报告 | 本次修复后验证 |
|------|---------------------|----------------|
| FlixPatrol | 0 | 120 |
| Merged candidates | 649 | 724 |
| P0 | 0 | 1 |
| P1 | 0 | 6 |
| P2 | 4 | 55 |
| P2+ 合计 | 4 | 62 |
| dry-run 写入 content_updates | 不适用 | 0（符合 dry-run） |

结论：FP 信号恢复后，P2+ 候选从 4 条提升到 62 条，符合 Phase 1.7 报告中“FP 正常日预计 P2+ 30-80 条”的预期区间。

---

## 数据库核验

验证后 `flixpatrol_top10` 的日期分布：

```text
2026-05-11 | 90
2026-05-13 | 120
```

`2026-05-13` 细分：

| 平台 | 类型 | 行数 |
|------|------|------|
| apple-tv-plus | movie | 10 |
| apple-tv-plus | tv_show | 10 |
| disney-plus | movie | 10 |
| disney-plus | tv_show | 10 |
| hbo-max | movie | 10 |
| hbo-max | tv_show | 10 |
| hulu | movie | 10 |
| hulu | tv_show | 10 |
| netflix | movie | 10 |
| netflix | tv_show | 10 |
| prime-video | movie | 10 |
| prime-video | tv_show | 10 |

结论：本次 dry-run 后，FP 当日原始数据已完整补齐为 6 平台 × 2 类型 × Top 10。

---

## 质量信号

本次验证同时完成了全量自动化测试：

```bash
PYTHONPATH=src .venv/bin/python -m pytest tests/ -v
```

关键结果：

```text
368 passed, 1 warning in 636.94s (0:10:36)
```

---

## 已发现问题

- [ ] OMDb 在端到端过程中对多条新查 IMDb ID 返回 `HTTP 401 Unauthorized`。
  - 现象：命令输出中出现多条 `OMDb lookup failed ... HTTP Error 401: Unauthorized`。
  - 影响：本次流程依靠已有缓存仍完成，最终 `omdb_hit=588`；但新条目的 OMDb 富化不可用。
  - 判断：这是独立于 FP 修复的外部 API 授权问题，应单独建任务处理。

- [ ] 本次为 `--dry-run`，没有写入 discovery 类 `content_updates`。
  - 输出 `written=0` 符合 dry-run 预期。
  - 原始源表 `flixpatrol_top10` 仍按现有设计补齐了 `2026-05-13` 的 FP 数据。

---

## 验收结论

- [x] FlixPatrol 不再为 0，`2026-05-13` 成功补齐 120 条。
- [x] 三源合并链路成功执行，merged candidates 为 724。
- [x] 评分过滤成功执行，P2+ 为 62 条。
- [x] 全量测试通过，368 passed。
- [ ] OMDb 401 未解决，需后续单独排查。

本次端到端验证通过 FP 修复相关验收，但不应视为 OMDb 富化链路完全健康。
