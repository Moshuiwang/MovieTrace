# MovieTrace 基线导入报告

状态：Phase 0 正式验证产物  
日期：2026-05-09  
数据来源：飞书多维表格 `节目` 表  
目标数据库：本地 SQLite `data/movietrace.db`

## 1. 导入结果

| 指标 | 数值 |
| --- | ---: |
| 飞书读取记录数 | 856 |
| 成功导入记录数 | 855 |
| 跳过记录数 | 1 |
| 当前 `baseline_items` 记录数 | 855 |
| 导入运行 ID | 1 |
| 导入状态 | success |

跳过原因：

- `skipped_empty_title=1`

## 2. 状态分布

| 状态 | 数量 |
| --- | ---: |
| 已下载 | 500 |
| 已上传FTP | 254 |
| 未知 | 101 |

说明：

- 当前导入逻辑按状态优先级解析：`已上架` > `已转码` > `已上传FTP` > `已下载` > `未知`。
- 本次读取中 `已上架` 和 `已转码` 没有 true 记录。

## 3. 内容粒度初步判断

| 粒度 | 数量 |
| --- | ---: |
| season | 741 |
| unknown | 114 |

说明：

- 当前仅根据标题中的 `Sxx` 模式做粗略判断。
- 该字段不能替代正式的 `content_granularity`。
- 后续完整节目库导出应优先补充内容类型、粒度、季号、集号和外部 ID。

## 4. 验证结论

本地 SQLite 已能承载飞书基线数据，飞书可以作为输入来源，本地数据库可以作为 Phase 0 后续匹配和去重的事实源。

后续建议：

1. 将 `baseline_items` 中的标题与 TMDb / Trakt 全量匹配结果写入 `match_candidates`。
2. 对 high 匹配抽样人工复核，确认准确率。
3. 将确认后的实体写入 `canonical_items` 和 `external_ids`。

