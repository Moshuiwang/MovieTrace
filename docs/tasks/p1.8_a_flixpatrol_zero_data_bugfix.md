# 任务包：P1.8-A FlixPatrol 当日数据为 0 回归修复

- [ ] 任务已审阅，同意执行（本次由用户在对话中直接授权执行）

**任务包版本：** v1
**创建时间：** 2026-05-13 22:01 +08

## 任务名称
P1.8-A FlixPatrol 当日数据为 0 回归修复

## 任务类型
fix

## 当前阶段
Phase 1.8：条件性调优前置修复

## 来源任务
用户要求：检查并修复 Phase 1.7 验收报告中 FlixPatrol 数据为 0 的问题。

## 目标
确保 `daily-discover` 为指定日期补齐并只使用该日期的 FlixPatrol 数据，避免因未来日期或 API 空结果导致当日 FP 信号静默缺失。

## 非目标
- ❌ 不调整 hot_score 权重。
- ❌ 不改 TMDb / Trakt / OMDb 采集逻辑。
- ❌ 不修改数据库 schema。

## 允许修改范围
- `src/movietrace/pipeline/discovery.py`
- `src/movietrace/sources/flixpatrol_api.py`
- `tests/test_discovery.py`
- `tests/test_flixpatrol_api.py`
- 本任务包文件

## 禁止修改范围
- 🚫 `AGENTS.md`、`CLAUDE.md`
- 🚫 数据库 migration / schema
- 🚫 Phase 1.8 评分策略和阈值

## 相关上下文
`data/movietrace.db` 中 `flixpatrol_top10` 仅有 `2026-05-11` 数据；`2026-05-13` 为 0。副本验证显示当前 FlixPatrol API 可返回并落库 `2026-05-13` 数据。

## 输入
- `/tmp/movietrace_phase0_secrets.json` 中的 FlixPatrol API key
- `data/movietrace.db`

## 输出
- 回归测试
- 最小代码修复
- 端到端验证结果

## 具体要求
- `_ensure_fp_data` 必须按 `snapshot_date = ?` 判断当日是否已有 FP 数据。
- FlixPatrol API 指定日期查询必须同时传入开始和结束日期，避免拉入后续日期。
- 当 API 调用完成但指定日期仍为 0 时，日志必须明确给出 warning。

## 验收标准
1. ✅ 有测试覆盖“未来日期数据不能让指定日期跳过补数”。
2. ✅ 有测试覆盖 FlixPatrol API 日期参数为同日闭区间。
3. ✅ `2026-05-13` 端到端 dry-run 能读到 FP 信号。

## 测试要求
新增或更新回归测试；全量测试通过。

## 验证命令
```bash
PYTHONPATH=src python -m pytest tests/test_discovery.py tests/test_flixpatrol_api.py -v
PYTHONPATH=src python -m pytest tests/ -v
PYTHONPATH=src python -m movietrace.cli daily-discover --date 2026-05-13 --dry-run
```

## 风险点
- FlixPatrol API 是外部服务，端到端真实运行可能受网络、配额或服务状态影响。
- 生产库补数会写入 `data/movietrace.db`；执行前需先通过单元测试。

## 完成后输出要求
按 [docs/workflow/report-format.md](../workflow/report-format.md) 格式汇报。
