# 任务包：P1.14-P1.17 V1 收口执行顺序

**任务包版本：** v1
**创建时间：** 2026-05-14 20:18 +08

## 任务名称
P1.14-P1.17：V1 收口执行顺序

## 任务类型
docs

## 当前阶段
V1 维护/收尾

## 来源任务
2026-05-14 用户询问“后续应该做什么、是否复盘、是否代码重构、是否升级工作方法”后的收口建议。

## 目标
记录 V1 收口阶段的任务顺序、依赖关系和不进入本批的范围。

## 非目标
- ❌ 不直接执行真实库升级。
- ❌ 不直接修改运行时代码。
- ❌ 不启动 V2。
- ❌ 不把可选代码卫生包装成必须执行的功能开发。

## 允许修改范围
- `docs/tasks/p1.14_to_p1.17_execution_order.md`
- `STATE.md`

## 禁止修改范围
- 🚫 `src/`
- 🚫 `tests/`
- 🚫 `data/`
- 🚫 AGENTS.md、CLAUDE.md

## 相关上下文
- Phase 1.13 已完成。
- 真实库仍需落盘 schema 14。
- 项目需要进入 V1 运行观察期。
- 文档体系需要降低默认上下文加载成本。

## 输入
- `STATE.md`
- `SCOPE.md`
- `docs/tasks/p1.14_true_db_schema14_smoke.md`
- `docs/tasks/p1.15_v1_closeout_operations_docs.md`
- `docs/tasks/p1.16_context_loading_slimdown.md`
- `docs/tasks/p1.17_daily_discover_code_hygiene.md`

## 输出
- 明确的 P1.14-P1.17 执行顺序。
- `STATE.md` 中可追溯的待执行任务清单。

## 具体要求
建议执行顺序：

```text
P1.14（真实库 schema 14 升级与 smoke 验收）
    ↓
P1.15（V1 收口复盘与运行手册）
    ↓
P1.16（上下文加载规则与文档瘦身）
    ↓
V1 运行观察 3-7 天
    ↓
P1.17（daily-discover 窄范围代码卫生，可选）
```

说明：
- P1.14 是真实运行前置条件，优先级最高。
- P1.15 把项目切到可运行、可交接、可评估状态。
- P1.16 降低后续 Agent 启动 token 成本，适合在收口文档稳定后执行。
- P1.17 只有在真实运行暴露维护痛点后才值得执行；没有痛点时不执行。
- V2 仍按 `SCOPE.md` 的触发条件判断，不在本批启动。

## 验收标准
1. ✅ P1.14-P1.17 任务包文件均存在。
2. ✅ `STATE.md` 能追溯到待执行清单和执行顺序。
3. ✅ P1.17 被明确标注为真实运行后可选任务。

## 测试要求
纯文档任务，执行结构检查即可。

## 验证命令
```bash
rg -n "P1.14|P1.15|P1.16|P1.17|V1 运行观察|执行顺序" STATE.md docs/tasks/p1.14_to_p1.17_execution_order.md docs/tasks/p1.14_true_db_schema14_smoke.md docs/tasks/p1.15_v1_closeout_operations_docs.md docs/tasks/p1.16_context_loading_slimdown.md docs/tasks/p1.17_daily_discover_code_hygiene.md
```

## 风险点
- 如果 P1.14 失败，后续文档可以继续草拟，但不能声明项目已进入真实运行观察期。
- 如果用户决定暂不改 AGENTS.md / CLAUDE.md，则 P1.16 需要先调整允许范围。
- P1.17 容易被误解为“必须重构”，执行前应重新确认真实维护痛点。

## 完成后输出要求
按 [docs/workflow/report-format.md](../workflow/report-format.md) 格式汇报。
