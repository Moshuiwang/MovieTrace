# 任务包：P1.26 修复"在播最新季"字段为空

**任务包版本：** v1
**创建时间：** 2026-05-18 18:00 +08

## 任务名称
P1.26 修复飞书"在播最新季"字段为空（discovery:tv 行 last_episode_to_air 丢失）

## 任务类型
fix

## 当前阶段
Phase 1 · 运行观察期（V1 已全量上线）

## 来源任务
GitHub issue #5：飞书"在播最新季"字段为空
计划文档：`/home/wang/.claude/plans/issues-sorted-hammock.md § 任务包 2`

## 目标
飞书"热点发现"子表"在播最新季"字段对 TV 内容永远有值（前提：TMDb 返回过 last_episode_to_air）。

## 非目标
- ❌ 不动 baseline_tracking.py（new_season 事件路径已经正常）
- ❌ 不动 discovery.py 候选合并逻辑
- ❌ 不动飞书 schema / 字段定义
- ❌ 不补写 OMDb / Trakt 的剧集信息（本任务只修 TMDb 路径）
- ❌ 不新增 api_cache 条目（回填从现有缓存读）

## 允许修改范围
- `src/movietrace/pipeline/omdb_enrichment.py`（仅 `_apply_tmdb_detail_data` 函数体，约 3 行）
- `tests/test_omdb_enrichment.py`（追加回归用例）
- 运行：`scripts/p1_24_backfill_in_play_season.py`（已存在，不改代码）

## 禁止修改范围
- 🚫 `src/movietrace/pipeline/discovery.py`
- 🚫 `src/movietrace/feishu/sync.py`（飞书侧已正确读 last_episode_to_air）
- 🚫 `src/movietrace/sources/tmdb.py`
- 🚫 `src/movietrace/db/`（不动 schema）
- 🚫 AGENTS.md / CLAUDE.md / STATE.md / SCOPE.md

## 相关上下文

### 根因（代码 + 数据已验证）

`src/movietrace/pipeline/omdb_enrichment.py:298-301` `_apply_tmdb_detail_data`：
```python
if not c.tmdb_data.get("last_episode_air_date"):
    lea = data.get("last_episode_to_air")
    if isinstance(lea, dict):
        c.tmdb_data["last_episode_air_date"] = str(lea.get("air_date", "")) if lea.get("air_date") else None
        # ↑ 只提取了 air_date 字符串，完整的 last_episode_to_air 对象被丢弃
```

`discovery.py:579` 读 `tmdb_data.get("last_episode_to_air")` → 永远 None（对象已丢失）。

`sync.py:281-282` 从 source_summary 读该字段 → 写空到飞书。

`baseline_tracking.py:164` 直接从 `sample_event.tmdb_details`（原始 detail 对象）读，不走 candidate，所以 new_season 事件能拿到值。

### 数据现状（data/movietrace.db 实测）

```sql
select count(*) from content_updates
where json_extract(source_summary_json, '$.last_episode_to_air') is not null;
-- 147 行（全部为 baseline-tracking 写的 new_season 事件）

select count(*) from content_updates;
-- 511 行总计 → 364 行该字段为空（全部 discovery:tv:* 行，待修复）
```

### 数据流
```
TMDb detail API → data["last_episode_to_air"] = {id, name, air_date, ...}
    ↓
_apply_tmdb_detail_data (omdb_enrichment.py:298-301)
    ↓ BUG: 只存 air_date 字符串，丢失完整对象
candidate.tmdb_data["last_episode_to_air"] = None（未被写入）
    ↓
discovery.py:579  summary["last_episode_to_air"] = c.tmdb_data.get("last_episode_to_air")  # → None
    ↓
content_updates.source_summary_json  # last_episode_to_air 不存在
    ↓
sync.py:281-282  → 飞书"在播最新季"字段空白
```

## 输入
- 现有 `_apply_tmdb_detail_data` 函数（`omdb_enrichment.py:266-305`）
- 现有回填脚本 `scripts/p1_24_backfill_in_play_season.py`（从 api_cache 读，不重复花 API 配额）

## 输出
- 修复后的 `_apply_tmdb_detail_data`：同时保留 `last_episode_air_date`（字符串）和 `last_episode_to_air`（完整对象）
- 新增回归测试用例
- 回填脚本运行日志（updated 数量 ≥ 1）

## 具体要求

1. **修改 `_apply_tmdb_detail_data`**：在现有 air_date 提取后，补一行保留完整对象：
   ```python
   if not c.tmdb_data.get("last_episode_air_date"):
       lea = data.get("last_episode_to_air")
       if isinstance(lea, dict):
           c.tmdb_data["last_episode_air_date"] = str(lea.get("air_date", "")) if lea.get("air_date") else None
           c.tmdb_data["last_episode_to_air"] = lea  # 新增：保留完整对象
   ```

2. **不改函数签名**：`_apply_tmdb_detail_data(c, data)` 保持不变。

3. **不删现有测试用例**，仅追加新用例。

4. **回填顺序**：先改代码 + 跑测试，再跑回填脚本，最后验证 content_updates 非空行数增加。

## 验收标准

1. ✅ `_apply_tmdb_detail_data` 处理含 `last_episode_to_air` dict 的 TMDb data 后，`c.tmdb_data["last_episode_to_air"]` 有值
2. ✅ 原有 `last_episode_air_date` 字符串提取逻辑不变（回归不破）
3. ✅ `scripts/p1_24_backfill_in_play_season.py` 运行后 updated 数量 > 0
4. ✅ `content_updates` 中 `last_episode_to_air` 非空行数 > 147（原为 147，理论上应达到 ~500+）
5. ✅ 全量测试不变红（baseline 535 passed，新增后 ≥ 537）

## 测试要求

`tests/test_omdb_enrichment.py` 追加（类名建议 `TestApplyTmdbDetailDataLastEpisode`）：

- `test_last_episode_to_air_object_preserved` — 输入含 `last_episode_to_air` dict，断言 `c.tmdb_data["last_episode_to_air"]` 等于原 dict
- `test_last_episode_air_date_string_still_extracted` — 同上输入，断言 `last_episode_air_date` 仍为字符串
- `test_last_episode_to_air_none_when_missing` — 输入不含该字段，断言不写入（不报 KeyError）

不需要 mock TMDb（纯函数，传 dict 即可）。不读写真实库。

## 验证命令

```bash
source .venv/bin/activate

# 单文件测试
PYTHONPATH=src python -m pytest tests/test_omdb_enrichment.py -v

# 回填脚本 dry-run
PYTHONPATH=src python scripts/p1_24_backfill_in_play_season.py --dry-run

# 回填脚本真跑
PYTHONPATH=src python scripts/p1_24_backfill_in_play_season.py

# 验证数据库行数变化
PYTHONPATH=src python -c "
import sqlite3
conn = sqlite3.connect('data/movietrace.db')
n = conn.execute(
    'select count(*) from content_updates where json_extract(source_summary_json, \"\$.last_episode_to_air\") is not null'
).fetchone()[0]
print('last_episode_to_air 非空行数:', n, '（修复前 147，期望 > 300）')
"

# 全量回归
PYTHONPATH=src python -m pytest tests/ -v --ignore=tests/test_flixpatrol_parsing.py
```

## 风险点

- **回填脚本依赖 api_cache**：若某 TV 内容的 TMDb detail 不在 cache 里（未跑过 enrich），回填会跳过该行，不会新增 API 调用。跳过的行需等下次 daily-discover 自然触发。
- **364 行全部回填不可保证**：只有 cache 命中的才能更新；实际 updated 数量取决于 cache 覆盖率。

## 完成后输出要求
按 [docs/workflow/report-format.md](../workflow/report-format.md) 格式汇报：
- 现象（364 行 discovery:tv 行 last_episode_to_air 为空）
- 已排除（不需要改 discovery.py / feishu sync / schema）
- 修改文件清单
- 验证结果（测试通过数 + 回填 updated 数量 + 数据库行数变化）
- 下一步（下次 daily-discover 覆盖 cache 未命中的行）
