# 任务包：P1.9-hotfix-C — baseline 多新季 local_max 回写错误

**任务包版本：** v1
**创建时间：** 2026-05-14 +08

## 任务名称
P1.9-hotfix-C：baseline tracking 多新季检测时 local_max_season 回写不完整

## 任务类型
fix

## 当前阶段
Phase 1.9：code review hotfix

## 来源任务
Code review CR-004（`reports/code_review_2026-05-14.md` § CR-004）

## 目标
当 TMDb 返回的季数比本地多 2 季以上时（如 local=1, tmdb=3），一次性将 `local_max_season` 更新到最大值，避免重复告警。

## 非目标
- ❌ 不改变新季检测逻辑
- ❌ 不重新设计 `content_updates` 去重规则
- ❌ 不回填历史错误数据

## 允许修改范围
- `src/movietrace/pipeline/baseline_tracking.py` → `run_baseline_tracking()`

## 禁止修改范围
- 🚫 AGENTS.md、CLAUDE.md
- 🚫 `detect_new_seasons()` 核心逻辑
- 🚫 schema

## 相关上下文
```python
# 当前逻辑：对同一 virtual_series 的多个新季事件，seen_vs 跳过后续事件
# local_max=1, tmdb_num_seasons=3 → 生成 S2/S3 两个事件
# update_local_max_season(conn, vs_id, 2) → 写到 2
# seen_vs 阻止 S3 的更新 → 下次仍检测到 S3

# 修复：按 vs_id 聚合，取最大 new_season_number 一次性回写
```

## 输入
- `src/movietrace/pipeline/baseline_tracking.py`

## 输出
- 修复后的 `run_baseline_tracking()`

## 具体要求
1. 在写 content_updates 后，按 `virtual_series_id` 分组取最大 `new_season_number`
2. 用最大值更新 `local_max_season`，而非每组的第一条
3. 保持 `seen_vs` 的防重逻辑（避免重复写 content_updates），但将 local_max 更新移出 seen_vs

## 验收标准
1. ✅ local=1, tmdb=3 时 local_max_season 被更新为 3
2. ✅ 单新季场景不退化（local=1, tmdb=2 → 更新为 2）
3. ✅ 全量测试通过

## 测试要求
- 更新 `tests/pipeline/test_baseline_tracking.py`：
  - 新增：local=1, tmdb_num=3 → local_max 更新为 3
  - 回归：local=1, tmdb_num=2 → local_max 更新为 2

## 验证命令
```bash
PYTHONPATH=src python -m pytest tests/pipeline/test_baseline_tracking.py -v
PYTHONPATH=src python -m pytest tests/ -v
```

## 风险点
- 低风险，局部聚合逻辑修复
