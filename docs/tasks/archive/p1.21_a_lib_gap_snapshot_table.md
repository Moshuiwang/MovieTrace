# 任务包：P1.21 A库缺口快照表

**任务包版本：** v1
**创建时间：** 2026-05-16 03:27 +08

## 任务名称

P1.21 A库缺口快照表 — 基于 DB 状态的飞书缺口视图

## 任务类型

feat

## 当前阶段

Phase 1.21

## 来源任务

ADR-0013（`docs/decisions/0013-baseline-gap-snapshot-table.md`）；背景：2026-05-16 content_updates 清理事故暴露了事件日志驱动视图的脆弱性。

## 目标

新建 Feishu 子表"A库缺口"，每行对应一个 virtual_series；内容从 DB 当前状态实时计算，不依赖 content_updates；通过新 CLI 命令 `sync-feishu-gap-table` 触发同步；集成到 `baseline_run.sh`。

## 非目标

- 集级缺口实现（仅预留字段）
- 修改 content_updates 表或 baseline-track 逻辑
- 自动归档或运营状态自动推进
- 删除"发现运行日志"现有子表

## 允许修改范围

- `src/movietrace/feishu/gap_sync.py`（新建）
- `src/movietrace/cli.py`（新增 `sync-feishu-gap-table` 命令）
- `scripts/baseline_run.sh`（新增 gap 同步步骤）
- `tests/test_gap_sync.py`（新建）
- `~/.config/movietrace/secrets.json`（新增 `feishu.gap_table_id`）
- `docs/decisions/0013-baseline-gap-snapshot-table.md`
- `docs/tasks/p1.21_a_lib_gap_snapshot_table.md`

## 禁止修改范围

- 🚫 `src/movietrace/pipeline/baseline_tracking.py`
- 🚫 `src/movietrace/feishu/sync.py`（discovery 同步不动）
- 🚫 `src/movietrace/db/schema.py`（不改 DB 结构）
- 🚫 `data/movietrace.db` 的 `content_updates` 表（只读辅助信息）
- 🚫 CLAUDE.md、AGENTS.md

## 相关上下文

- `virtual_series`：id, tmdb_tv_id, name, tmdb_status, tmdb_number_of_seasons, local_max_season, poll_priority
- `canonical_items`（关联 vs）：season_number, content_type
- `external_ids` source='upstream'：标记已入 A库
- A库 max season：`MAX(ci.season_number) WHERE upstream external_id exists`
- TMDb aired season：`json_extract(api_cache.response_json, '$.last_episode_to_air.season_number')` 其中 `cache_key = 'tmdb:detail:' || vs.tmdb_tv_id || ':tv'`
- auth 复用：`from movietrace.feishu.baseline import fetch_tenant_access_token`
- upsert key：TMDb ID（全局唯一）

## 输入

- `data/movietrace.db`（virtual_series, canonical_items, external_ids, api_cache, content_updates）
- `~/.config/movietrace/secrets.json`（feishu 凭据 + 新 gap_table_id）

## 输出

- Feishu 子表"A库缺口"（rows = virtual_series with gap > 0, poll_priority != 'skip'）
- `src/movietrace/feishu/gap_sync.py`
- `tests/test_gap_sync.py`
- 更新后的 `cli.py`、`baseline_run.sh`

## 具体要求

### SQL 逻辑

```sql
WITH a_lib_max AS (
  SELECT ci.virtual_series_id AS vs_id, MAX(ci.season_number) AS a_lib_max_season
  FROM canonical_items ci
  JOIN external_ids ei ON ei.canonical_item_id = ci.id AND ei.source = 'upstream'
  WHERE ci.virtual_series_id IS NOT NULL
  GROUP BY ci.virtual_series_id
),
tmdb_aired AS (
  SELECT vs.id AS vs_id,
         CAST(json_extract(ac.response_json, '$.last_episode_to_air.season_number') AS INTEGER) AS tmdb_aired_season
  FROM virtual_series vs
  LEFT JOIN api_cache ac ON ac.cache_key = 'tmdb:detail:' || vs.tmdb_tv_id || ':tv'
),
recent_hot AS (
  SELECT ci.virtual_series_id AS vs_id, MAX(cu.hot_score) AS hot_score
  FROM content_updates cu
  JOIN canonical_items ci ON ci.id = cu.canonical_item_id
  WHERE cu.created_at >= datetime('now', '-30 days')
  GROUP BY ci.virtual_series_id
)
SELECT vs.id AS vs_id, vs.name, vs.tmdb_tv_id, vs.tmdb_status,
       COALESCE(alm.a_lib_max_season, 0) AS a_lib_max,
       ta.tmdb_aired_season,
       ta.tmdb_aired_season - COALESCE(alm.a_lib_max_season, 0) AS gap_count,
       COALESCE(rh.hot_score, 0) AS hot_score
FROM virtual_series vs
LEFT JOIN a_lib_max alm ON alm.vs_id = vs.id
LEFT JOIN tmdb_aired ta ON ta.vs_id = vs.id
LEFT JOIN recent_hot rh ON rh.vs_id = vs.id
WHERE vs.poll_priority != 'skip'
  AND ta.tmdb_aired_season IS NOT NULL
  AND ta.tmdb_aired_season > COALESCE(alm.a_lib_max_season, 0)
ORDER BY COALESCE(rh.hot_score, 0) DESC, gap_count DESC;
```

### gap_seasons 字符串

Python 中构造：`",".join(f"S{s}" for s in range(a_lib_max + 1, tmdb_aired + 1))`

### upsert 策略

1. `records/search`（无 filter，分页）拉取全部已有行，按 TMDb ID 建映射 `{tmdb_id: record_id}`
2. 对每个 gap row：若已有 → PUT 更新（不改 `运营状态`、`备注`）；若无 → POST 创建（默认 `运营状态=待补`）

### 系统提示字段

创建时默认 `-`；更新时若 gap=0 写"建议已补"（本次 SQL 已过滤 gap=0，实际该字段本次全为 `-`；预留逻辑供未来 gap→0 时用）。

### CLI 命令

`sync-feishu-gap-table [--dry-run]`；不需要 `--source`（直接读 DB）。

### baseline_run.sh 集成

在 `sync-feishu-table` 步骤之后新增 gap 同步步骤；GAP_EXIT 纳入退出码聚合。

## 验收标准

1. ✅ `tests/test_gap_sync.py` 3 个测试全部通过
2. ✅ `PYTHONPATH=src .venv/bin/python -m pytest tests/ -q` 全量 502+ 通过
3. ✅ Feishu 子表"A库缺口"创建成功（有效 table_id 写入 secrets.json）
4. ✅ `sync-feishu-gap-table` 运行后 Feishu 子表有行写入（预期 135-160 行）
5. ✅ `baseline_run.sh` 末尾包含 gap 同步步骤和 GAP_EXIT

## 测试要求

`tests/test_gap_sync.py`，使用 in-memory SQLite + `initialize_database`：

1. `test_compute_gaps_basic` — vs A库 S3，TMDb S5 → gap_count=2，gap_seasons="S4,S5"
2. `test_compute_gaps_no_gap_excluded` — vs A库 S5，TMDb S5 → 结果为空
3. `test_compute_gaps_skip_priority_excluded` — poll_priority='skip' → 结果为空

## 验证命令

```bash
# 单元测试
PYTHONPATH=src .venv/bin/python -m pytest tests/test_gap_sync.py -v

# 全量测试
PYTHONPATH=src .venv/bin/python -m pytest tests/ -q

# dry-run 验证 SQL 输出（无飞书网络请求）
PYTHONPATH=src .venv/bin/python -m movietrace.cli sync-feishu-gap-table --dry-run

# 真实同步
PYTHONPATH=src .venv/bin/python -m movietrace.cli sync-feishu-gap-table 2>&1
```

## 风险点

- Feishu `records/search` 无 filter 分页拉取量可能较大（预期 <200 行，500/页够）
- `api_cache` 中若无某 vs 的 TMDb detail 缓存，该 vs 将被排除（tmdb_aired_season IS NOT NULL 过滤），符合预期
- `运营状态` 字段更新时需跳过（不覆盖人工填写值），upsert 时区分新建/更新逻辑

## 完成后输出要求

按 `docs/workflow/report-format.md` 格式汇报。
