# 任务包：P1.9-hotfix-B — TV freshness last_air_date 数据未落地

**任务包版本：** v1
**创建时间：** 2026-05-14 +08

## 任务名称
P1.9-hotfix-B：TV freshness 的 last_air_date 数据链路修复

## 任务类型
fix

## 当前阶段
Phase 1.9：code review hotfix

## 来源任务
Code review CR-003（`reports/code_review_2026-05-14.md` § CR-003）

## 目标
让 TV 候选的 `last_air_date` 真正进入评分链路。当前 trending/popular API 不返回此字段，必须通过 TMDb detail 补充。

## 非目标
- ❌ 不修改 trending/popular 采集逻辑（它们确实没有 last_air_date）
- ❌ 不修改评分公式
- ❌ 不修改 `_to_scoring_dict` 的 fallback 链（P1.8-C 已实现，只需要数据到达）

## 允许修改范围
- `src/movietrace/pipeline/omdb_enrichment.py` → `enrich_with_tmdb_details()`
- `tests/test_omdb_enrichment.py`

## 禁止修改范围
- 🚫 AGENTS.md、CLAUDE.md
- 🚫 `scoring.py`
- 🚫 `multi_source_merge.py`
- 🚫 `tmdb.py` / `tmdb_trending.py`

## 相关上下文
- P1.8-C 已在 schema、`normalize_tmdb_trending_row`、`_to_scoring_dict` 中实现 `last_air_date` 的字段支持和 fallback 逻辑
- 但 trending/popular payload 本身不包含 `last_air_date`（只有 TMDb detail 有）
- 当前 `enrich_with_tmdb_details()` 在候选已有 `release_date` + `original_language` 时跳过 detail
- DB 验证：`select sum(last_air_date is not null) from tmdb_trending` → 0
- 需要针对 TV 候选调整跳过条件

## 输入
- `data/movietrace.db`
- TMDb detail API 响应

## 输出
- 修复后的 `enrich_with_tmdb_details()`

## 具体要求
1. `enrich_with_tmdb_details()` 跳过条件增加：TV 候选缺 `last_air_date` 时仍调 detail
2. detail 返回后回写 `c.tmdb_data["last_air_date"]` / `last_episode_air_date` 等字段
3. 电影候选行为不变（仍跳过已有 release_date + language 的）
4. 不增加不必要的 API 调用

## 验收标准
1. ✅ TV 候选缺 last_air_date 时触发 TMDb detail
2. ✅ detail 返回后 c.tmdb_data 含 last_air_date
3. ✅ 电影候选不触发额外 detail
4. ✅ 全量测试通过

## 测试要求
- 更新 `tests/test_omdb_enrichment.py`：
  - TV 候选缺 last_air_date 时调 detail
  - TV 候选已有 last_air_date 时跳过
  - 电影候选行为不变

## 验证命令
```bash
PYTHONPATH=src python -m pytest tests/test_omdb_enrichment.py -v
PYTHONPATH=src python -m pytest tests/ -v
```

## 风险点
- 会增加 TMDb detail API 调用（仅针对 TV 候选），API 配额需关注
- 当前 OMDb/FP 已不可用，TMDb 是唯一正常服务，需控制增幅
