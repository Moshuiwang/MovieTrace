# 任务包：P1.9-hotfix-F — Hulu→Paramount+ 默认值同步

**任务包版本：** v1
**创建时间：** 2026-05-14 +08

## 任务名称
P1.9-hotfix-F：评分默认值从 Hulu 同步到 Paramount+

## 任务类型
fix

## 当前阶段
Phase 1.9：code review hotfix

## 来源任务
Code review CC-005（`reports/code_review_2026-05-14.md` § CC-005）

## 目标
P1.8-H 已将 FP 平台策略从 Hulu 切到 Paramount+，但评分权重和 platform fallback 仍使用 Hulu 默认值。

## 非目标
- ❌ 不调整权重数值
- ❌ 不修改 `config/scoring_weights.yaml`
- ❌ 不改变优先级阈值

## 允许修改范围
- `src/movietrace/pipeline/scoring.py` → `DEFAULT_WEIGHTS`
- `src/movietrace/pipeline/discovery.py` → `_to_scoring_dict` 的 platform fallback

## 禁止修改范围
- 🚫 AGENTS.md、CLAUDE.md
- 🚫 `config/scoring_weights.yaml`
- 🚫 FlixPatrol 客户端

## 相关上下文
- P1.8-H 在 `flixpatrol_api.py` 中 PLATFORM_COMPANY_IDS 已移除 Hulu、增加 Paramount+
- 但 `scoring.py` DEFAULT_WEIGHTS 仍有 `hulu: 0.8` 无 `paramount-plus`
- `discovery.py` 中 platform fallback 仍是 `"hulu"`
- config.yaml 的 flixpatrol.platforms 不含 Hulu

## 输入
- `scoring.py` DEFAULT_WEIGHTS
- `discovery.py` `_to_scoring_dict`

## 输出
- 同步后的默认值

## 具体要求
1. `DEFAULT_WEIGHTS["platform_weight"]` 增加 `"paramount-plus": 0.80`
2. Hulu 保留在权重字典中（兼容历史 FP 数据中的 hulu 条目）
3. `_to_scoring_dict` 的 platform fallback 从 `"hulu"` 改为 `"unknown"`

## 验收标准
1. ✅ DEFAULT_WEIGHTS 含 paramount-plus 权重
2. ✅ platform fallback 为 "unknown" 而非 "hulu"
3. ✅ Hulu 权重不丢失（兼容历史数据）
4. ✅ 全量测试通过

## 测试要求
- 更新 scoring 测试中的平台权重检查

## 验证命令
```bash
PYTHONPATH=src python -m pytest tests/test_scoring.py -v
PYTHONPATH=src python -m pytest tests/ -v
```

## 风险点
- 几乎无风险，纯常量更新
