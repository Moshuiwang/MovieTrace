# 任务包：daily-discover 进度格式化与飞书通知集成

**任务包版本：** v1
**创建时间：** 2026-05-20 +08

## 任务名称
P1.37 progress-format-notify — [1/8] 步骤进度输出 + enrichment 细节统计 + 飞书卡片集成

## 任务类型
feat

## 当前阶段
Phase 1

## 来源任务
本次会话设计讨论：现有进度输出为 [1/5] 简单文本，缺乏 enrichment 子步骤细节；
飞书卡片不包含步骤进度信息；FP 抓取在输出中不可见。
依赖 P1.36 完成后才能执行（FP 需在 CLI 层有独立结果）。

## 目标
1. `cmd_daily_discover()` 进度输出重构为 [1/8] 格式（含 enrichment 子步骤 + gzip 错误折叠）
2. `_compute_discovery_stats()` 返回值加入 enrichment 子步骤细节字段
3. 飞书卡片（`_build_card()`）新增"运行进度"section，展示该格式化摘要

## 非目标
- ❌ 不改飞书 Bitable 同步逻辑
- ❌ 不改 scoring / filtering 逻辑
- ❌ 不新增 CLI 命令
- ❌ 不引入新依赖

## 允许修改范围
- `src/movietrace/cli.py`（`cmd_daily_discover()` 进度打印部分）
- `src/movietrace/pipeline/discovery.py`（`_compute_discovery_stats()` 返回字段扩展）
- `src/movietrace/feishu/notify.py`（`_build_card()` 新增 section）

## 禁止修改范围
- 🚫 `src/movietrace/feishu/sync.py`
- 🚫 `src/movietrace/pipeline/scoring.py`
- 🚫 `src/movietrace/sources/` 所有文件
- 🚫 AGENTS.md、CLAUDE.md

## 相关上下文
**确认的进度格式**（8 步）：
```
[1/8] 拉取 FlixPatrol 榜单数据...
  ⚠️  API Key 未配置，使用缓存快照 (2026-05-13, 120 条)

[2/8] 拉取 TMDb 热门数据...
  ⚠️  共 3 个端点失败: utf-8 解码错误（gzip 未解压），使用缓存快照 (2026-05-19)

[3/8] 拉取 Trakt 热门数据...
  ✓  新鲜数据: 40 条 (剧集 20 · 电影 20)

[4/8] 数据源日期回退决策...
  FlixPatrol → 2026-05-13 (fallback)
  TMDb       → 2026-05-19 (fallback)
  Trakt      → 2026-05-20 (fresh)

[5/8] 三源合并去重...
  ✓  fp=120  tmdb=60  trakt=40  →  候选 165 条

[6/8] 候选内容丰富化...
  IMDb ID 回填:  补全 20 / 165 条  ⚠️  共 8 条失败: utf-8 解码错误（gzip 未解压）
  OMDb 评分:     丰富化 143 / 165 条  (API 17 次 · 缓存命中 138 次)
  TMDb 详情:     丰富化 136 / 165 条  (API 2 次 · 缓存命中 134 次)  ⚠️  共 5 条失败: gzip

[7/8] 预估引进时长计算...
  ✓  98 条候选处理完成 (0.6s)
     电影: 固定 2.0h / 部
     剧集: 平均缺 14 集 / 约 14h per 条目

[8/8] 热度打分 · 过滤 · 写入...
  ✓  打分 165 条，Soap 自动降权 2 条
  ✓  通过 P2+ 阈值: 98 / 165 条  (P0=2  P1=5  P2=91)
  ✓  新注册 3 条，写入 content_updates 98 条

=== 运行摘要 ===
结束: 2026-05-20 08:01:20 +08  耗时: 79s
退出码: 1 | discover=OK  export=OK  sync=FAIL

⚠️  根因: utf-8 解码错误（gzip 未解压），影响 TMDb + IMDb 共 16 条
```

**enrichment 子步骤细节**（需加入 discover_stats）：
- `enrich_imdb_backfill`: `{backfilled, total, errors}` — 已有，在 `enrich_stats["imdb_backfill"]`
- `enrich_omdb`: `{enriched, api_calls, cache_hits, errors}` — 已有
- `enrich_tmdb_detail`: `{enriched, api_calls, cache_hits, errors}` — 已有
- gzip 错误数：从各 enrich_stats 的 `errors` 字段读取，折叠展示

**飞书卡片集成**：在 `_build_card()` 现有 section 后新增一个 section，
内容为上述格式化进度的纯文本摘要（不含时间戳，只含步骤结果行）。

## 输入
- P1.36 完成后的 `src/movietrace/cli.py`（FP 已提出到 CLI 层）
- P1.35 完成后的 `src/movietrace/sources/http.py`（gzip 已修复）
- `--discover-stats-file` JSON 的现有结构

## 输出
- 修改后的 `src/movietrace/cli.py`
- 修改后的 `src/movietrace/pipeline/discovery.py`（stats 字段扩展）
- 修改后的 `src/movietrace/feishu/notify.py`

## 具体要求

### A. discovery.py — _compute_discovery_stats() 扩展
在返回 dict 中新增（这些数据已在 `enrich_stats` 里，只是需要透传出来）：
```python
"enrich_imdb_backfill": enrich_stats.get("imdb_backfill", {}),  # 已有，确认透传
"enrich_omdb": enrich_stats.get("omdb", {}),                    # 已有，确认透传
"enrich_tmdb_detail": enrich_stats.get("tmdb_detail", {}),      # 已有，确认透传
```
同时将 `fp_fetch_result` 的统计（fallback/fresh/error）也纳入返回值，
供 cli.py 格式化步骤 [1/8]。

### B. cli.py — 进度输出重构
- 用 8 步格式替换现有 [1/5] 输出
- 同根因 gzip 错误折叠（同一步骤内多条 "gzip" 错误合并为一行 `⚠️ 共 N 条失败: gzip 未解压`）
- 步骤 [7/8] 计算剧集平均缺集数 / 平均时长并输出
- "运行摘要"块在最后打印，包含退出码标签

### C. notify.py — _build_card() 新增 section
- 在现有卡片末尾 action buttons 之前，新增一个 div section
- 内容：步骤 [1/8]~[8/8] 的单行结果摘要（每步一行，不含子细节）
- 格式：lark_md，用 `✓` / `⚠️` / `❌` 前缀

## 验收标准
1. ✅ `PYTHONPATH=src python -m movietrace.cli daily-discover --dry-run` 输出符合 [1/8] 格式
2. ✅ `discover_stats` JSON 包含 `enrich_imdb_backfill` / `enrich_omdb` / `enrich_tmdb_detail` 字段
3. ✅ `PYTHONPATH=src python -m pytest tests/ -v` 全量通过，无新失败
4. ✅ 飞书卡片 `_build_card()` 单元测试：新 section 出现在 elements 列表中

## 测试要求
- 新增测试验证 `_compute_discovery_stats()` 返回包含 enrichment 细节字段
- 现有卡片构建测试通过

## 验证命令
```bash
source .venv/bin/activate
# 针对性测试（~10s）——覆盖改动涉及的模块
PYTHONPATH=src python -m pytest tests/test_discovery.py tests/test_notify.py tests/test_http_client.py -v

# 结构验证（<1s）——确认 CLI 能正常 import 和解析参数
PYTHONPATH=src python -c "from movietrace.cli import cmd_daily_discover; print('CLI import OK')"

# 全量回归（~70s）——仅在提 PR 前跑一次
PYTHONPATH=src python -m pytest tests/ -v
```

## 风险点
- `--discover-stats-file` JSON 结构变化会影响 `notify-feishu` 读取，需确认向后兼容（新增字段，不删旧字段）
- 步骤 [7/8] 的"平均缺集"需要 passed 集合里的 `row_duration_hours`，dry-run 下可能为 0，显示 N/A 即可
- 飞书卡片字符数有上限（约 30000 字符），新增 section 内容要简短

## 依赖
- **必须先完成 P1.36**（FP fetch 提出后，[1/8] 才能有 FP stats）
- P1.35 完成后 gzip 错误消失，[6/8] 的错误折叠行变为正常输出，格式仍然兼容

## 完成后输出要求
按 docs/workflow/report-format.md 格式汇报。
完成后按 CLAUDE.md § Git 工作流交付（`feat/p1.37-progress-format` → PR → auto-merge）。
